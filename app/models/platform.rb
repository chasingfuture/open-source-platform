# Not a real model.
# Just contain some constants to keep track of known & supported repositories platforms.
# This is kept as a model rather than some constants in an initializer if we later want to switch it into a model.
module Platform

  # individual platforms
  GITHUB  = 'github'

  # array containing all platforms
  EXT_PLATFORMS = [ GITHUB ]

end
